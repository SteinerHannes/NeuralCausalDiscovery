# @package _global_
defaults:
  - _base
  - override /data: ForkColliderNonlinearSEM
  - override /model: ShallowMLP
  - override /causaldiscovery/ci_test: CondIndepKCI
  - _self_

experiment:
  name: fork_collider_nonlinear_sem_shallow_mlp_align

extraction:
  alignment:
    enabled: true
