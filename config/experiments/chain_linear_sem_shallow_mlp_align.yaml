# @package _global_
defaults:
  - _base
  - override /data: ChainLinearSEM
  - override /model: ShallowMLP
  - override /causaldiscovery/ci_test: CondIndepParCorr
  - _self_

experiment:
  name: chain_linear_sem_shallow_mlp_align

extraction:
  alignment:
    enabled: true
