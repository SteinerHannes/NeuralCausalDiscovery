# @package _global_
defaults:
  - _base
  - override /data: ElectricalCircuitData
  - override /model: ShallowMLP
  - override /causaldiscovery/ci_test: CondIndepKCI
  - _self_

experiment:
  name: electrical_circuit_io_3_3_shallow_mlp_align

extraction:
  alignment:
    enabled: true

data:
  input_size: 3
  output_size: 3
