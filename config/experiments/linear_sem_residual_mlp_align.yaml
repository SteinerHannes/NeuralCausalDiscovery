# @package _global_
defaults:
  - _base
  - override /data: LinearSEMDataset
  - override /model: ResidualMLP
  - override /causaldiscovery/ci_test: CondIndepParCorr
  - _self_

experiment:
  name: linear_sem_residual_mlp_align

extraction:
  alignment:
    enabled: true
