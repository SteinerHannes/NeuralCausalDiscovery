# @package _global_
defaults:
  - _base
  - override /data: LinearSEMDataset_nodes_7
  - override /model: ShallowMLP
  - override /causaldiscovery/ci_test: CondIndepParCorr
  - _self_

experiment:
  name: linear_sem_nodes_7_shallow_mlp_align
  metadata:
    appendix_only: true

extraction:
  alignment:
    enabled: true
