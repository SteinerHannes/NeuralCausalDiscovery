# @package _global_
defaults:
  - _base
  - override /data: LayeredSparseNonlinearSEM
  - override /model: GatedMLP
  - override /causaldiscovery/ci_test: CondIndepKCI
  - _self_

experiment:
  name: layered_sparse_nonlinear_sem_gated_mlp_align

extraction:
  alignment:
    enabled: true
