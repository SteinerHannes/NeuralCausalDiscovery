# @package _global_
defaults:
  - _base
  - override /data: LayeredSparseLinearSEM
  - override /model: BottleneckMLP
  - override /causaldiscovery/ci_test: CondIndepParCorr
  - _self_

experiment:
  name: layered_sparse_linear_sem_bottleneck_mlp_align

extraction:
  alignment:
    enabled: true
