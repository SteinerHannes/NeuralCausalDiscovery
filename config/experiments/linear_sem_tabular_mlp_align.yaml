# @package _global_
defaults:
  - _base
  - override /data: LinearSEMDataset
  - override /model: TabularMLP
  - override /causaldiscovery/ci_test: CondIndepParCorr
  - _self_

experiment:
  name: linear_sem_tabular_mlp_align

extraction:
  alignment:
    enabled: true
