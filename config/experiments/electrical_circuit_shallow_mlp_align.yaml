# @package _global_
defaults:
  - _base
  - override /data: ElectricalCircuitData
  - override /model: ShallowMLP
  - override /causaldiscovery/ci_test: CondIndepKCI
  - _self_

experiment:
  name: electrical_circuit_shallow_mlp_align

extraction:
  alignment:
    enabled: true
