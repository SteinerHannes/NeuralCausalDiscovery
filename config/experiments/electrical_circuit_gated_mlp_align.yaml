# @package _global_
defaults:
  - _base
  - override /data: ElectricalCircuitData
  - override /model: GatedMLP
  - override /causaldiscovery/ci_test: CondIndepKCI
  - _self_

experiment:
  name: electrical_circuit_gated_mlp_align

extraction:
  alignment:
    enabled: true
